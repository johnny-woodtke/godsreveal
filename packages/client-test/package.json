{
  "name": "@godsreveal/client-test",
  "version": "0.1.6",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json && tsc-alias",
    "lint": "eslint --fix .",
    "release:dry": "npm pack && tar -xvzf *.tgz && rm -rf package *.tgz",
    "release": "npm publish --access public"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@godsreveal/eslint-config": "workspace:*",
    "@godsreveal/typescript-config": "workspace:*",
    "@types/node": "^20",
    "eslint": "^8",
    "eslint-config-next": "14.2.6",
    "typescript": "5.5.4",
    "tsc-alias": "^1.8.8"
  }
}
